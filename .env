# ── Local `act` testing only ──────────────────────────────────────────────────
# These values are used when running the workflow locally via `act`.
# In real CI, MR_PROJECT_ID and CF_PROJECT_ID come from GitHub repo variables
# (Settings → Secrets and variables → Actions → Variables).
MR_PROJECT_ID=e0oMrxjp
CF_PROJECT_ID=
GITHUB_REF=refs/tags/v3.0.0
